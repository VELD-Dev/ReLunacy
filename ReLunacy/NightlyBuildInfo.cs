namespace ReLunacy;

// Stamped by .github/workflows/nightly.yml with the commit hash and build date for a nightly
// build (see UpdateChecker.CheckNightly). Null for every other build.
public static class NightlyBuildInfo
{
    public const string? CommitHash = null;
    public const string? BuildDate = null;
}
